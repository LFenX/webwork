import { ModulePageLoading } from "@/components/loading/app-loading-states"

export default function WebsiteFolderLoading() {
  return <ModulePageLoading maxWidth="full" stats={3} rows={8} table={false} />
}
